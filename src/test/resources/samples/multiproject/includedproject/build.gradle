plugins {
    id 'base'
    id 'com.dorongold.task-tree'
}

tasks.register('taskInIncludedProject') {
    doLast {
        println 'I am a task-in-included-project'
    }
    dependsOn(tasks.named('build'))
}

